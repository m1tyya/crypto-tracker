import { Inter, Raleway } from '@next/font/google';

export const inter = Inter({ subsets: ['latin'], weight: ['400', '500', '600'] });
export const raleway = Raleway({ subsets: ['latin'], weight: ['700'] });
